/** Re-export the per-article OG image as the Twitter card so each article
 * gets the same editorial preview on both networks. */
export {
  default,
  alt,
  contentType,
  size,
  generateStaticParams,
} from "./opengraph-image";
